import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  container: {
    marginTop: 20,
    textAlign: 'center',
  },
  title: {
    marginTop:20,
    color: '#3f51b5',
    fontWeight: '900',
    fontSize: '28px',
  }
}))

export default useStyles